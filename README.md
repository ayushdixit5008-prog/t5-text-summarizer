# t5-text-summarizer
Built an abstractive text summarization system using the pretrained T5-Small Transformer. Includes data preprocessing, tokenizer preparation, model fine-tuning, training, evaluation, model saving/loading, and FastAPI deployment to provide fast and efficient text summarization through a web API.
